import 'package:flutter/material.dart';

class TypeLearningScreen extends StatelessWidget {
  const TypeLearningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TypeLearningScreen');
  }
}